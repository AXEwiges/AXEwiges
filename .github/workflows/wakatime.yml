name: Waka Readme

on:
  workflow_dispatch:
  schedule:
    # Runs at every 12AM UTC
    - cron: "50 14 * * *"

jobs:
  update-readme:
    name: Update this repo's README
    runs-on: ubuntu-latest
    steps:
      - uses: AXEwiges/AXEwiges@main
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
